#include <stdio.h>

int main()
{
    int numero, numero2;
    int temp = 0;
    do
    {
        scanf("%d", &numero);
    }
    while (numero < 0 || numero > 20);

    for (int i = 0; )
    {
        numero += temp;
        temp = numero2;
        numero2++;
    }
}